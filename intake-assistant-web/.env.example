# VITE_API_URL: set only for local dev (npm run dev) without nginx proxy.
#   Local dev (direct): http://localhost:8000
#   k3d / production:   leave unset (uses relative path, nginx proxies /api/)
VITE_API_URL=http://localhost:8000

VITE_SDWC_WEB_URL=http://localhost:3001
